extends layout

block content
  h1= title
  p Welcome to #{title}
  form(method='post', action='/users/new', id="newUserForm")
    | What is your name?
    input(name='name', id="nameinput")
    button('type='submit')
      | Done